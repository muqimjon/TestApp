<Page x:Class="TestApp.Desktop.Views.Pages.TestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestApp.Desktop.Views.Pages"
      mc:Ignorable="d" 
      Title="TestPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ItemsControl x:Name="QuestionNumbers" 
                              ItemsSource="{Binding QuestionNumbers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <TextBlock x:Name="QuestionText"
                       FontSize="18"
                       TextWrapping="Wrap"/>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Button x:Name="OptionA" Content="A) ..." 
                    Margin="0,5" FontSize="16" Height="40"/>
            <Button x:Name="OptionB" Content="B) ..." 
                    Margin="0,5" FontSize="16" Height="40"/>
            <Button x:Name="OptionC" Content="C) ..." 
                    Margin="0,5" FontSize="16" Height="40"/>
            <Button x:Name="OptionD" Content="D) ..." 
                    Margin="0,5" FontSize="16" Height="40"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10">
            <Button Content="⬅ Oldingi" Width="100" Margin="10"/>
            <TextBlock x:Name="ProgressText" 
                       VerticalAlignment="Center" FontSize="16"/>
            <Button Content="Keyingi ➡" Width="100" Margin="10"/>
        </StackPanel>
    </Grid>
</Page>
